[see test code](#docs/code/h5_structure_demo.py)

H5 文件结构：
┌─────────────────────────┐
│  cropped_volume_ROI.h5  │
├─────────────────────────┤
│  "data" (数据集名称)     │
│  ┌───────────────────┐  │
│  │ 3D 数组           │  │
│  │                   │  │
│  │  D 层切片         │  │
│  │  ┌─────┐          │  │
│  │  │ H×W │  ← 每层  │  │
│  │  └─────┘          │  │
│  │  ┌─────┐          │  │
│  │  │ H×W │          │  │
│  │  └─────┘          │  │
│  │  ...              │  │
│  │  ┌─────┐          │  │
│  │  │ H×W │          │  │
│  │  └─────┘          │  │
│  └───────────────────┘  │
│  形状: (D, H, W)         │
└─────────────────────────┘



修改 h5_structure_demo.py，使用项目中实际的 H5 文件数据：
查找项目中的实际 H5 文件：
修改代码，使用项目中实际的 H5 文件：
继续修改文件的剩余部分：
修复一个小问题：
运行修改后的代码，查看效果：
$ cd /Users/jy/Desktop/1_Personal_Proj/19_qt/DeepMatter-RandD-6.2 && python h5_structure_demo.py
================================================================================
H5 文件结构演示（使用项目实际数据）
================================================================================

【步骤 1】读取项目中的实际 H5 文件
--------------------------------------------------------------------------------
文件路径: data/project_test/history_001/preprocess/full/cropped_volume_FULL.h5
文件大小: 87.23 MB

【步骤 2】检查 H5 文件结构
--------------------------------------------------------------------------------
H5 文件中的键（keys）: ['data']

数据集 'data' 的详细信息:
  形状: (538, 466, 466)
  维度: 3D
  数据类型: uint8
  压缩方式: gzip
  数据大小（未压缩）: 111.42 MB
  实际文件大小（压缩后）: 87.23 MB
  压缩率: 21.7%

【步骤 3】读取数据样本
--------------------------------------------------------------------------------
第一层切片 (dataset[0, :, :]):
  形状: (466, 466)
  数据类型: uint8
  值范围: [10, 255]
  平均值: 86.04

第一层切片的前10x10区域:
[[ 75  80  91  99  95  84  79  83  86  97]
 [ 81  85  97  93  92  93  97  86  79  86]
 [101 102 106  90  95  84 106  80  65  79]
 [ 95  81  86  79 112 126 103  77  69  59]
 [ 92  81  81  89 100 104 102  83  60  39]
 [102  93  92  99  93  96  90  82  45  50]
 [ 96 105 102  98  73  77  88  62  42  41]
 [ 82 101 102  85  76  75  64  50  42  50]
 [ 85 104  88  73  75  54  40  31  52  58]
 [ 93  85  80  72  65  40  34  34  42  45]]

中间层切片 (dataset[269, :, :]):
  形状: (466, 466)
  值范围: [17, 147]
  平均值: 85.69

最后一层切片 (dataset[-1, :, :]):
  形状: (466, 466)
  值范围: [8, 220]
  平均值: 85.93

【步骤 4】读取完整 3D 数组
--------------------------------------------------------------------------------
数据量较小，读取完整数组...
  完整数组形状: (538, 466, 466)
  完整数组类型: <class 'numpy.ndarray'>
  完整数组数据类型: uint8
  完整数组值范围: [0, 255]
  完整数组平均值: 85.99
  完整数组中位数: 89.00

【步骤 5】3D 数组维度说明
--------------------------------------------------------------------------------
形状 (D, H, W) = (538, 466, 466)
  D (深度/切片数) = 538 层
  H (高度) = 466 像素
  W (宽度) = 466 像素
  总像素数: 116,829,928
  每层切片大小: 466 × 466 = 217,156 像素

【步骤 6】H5 文件的特点
--------------------------------------------------------------------------------
H5 文件（HDF5 格式）的特点：
  1. ✅ 可以存储任意维度的 NumPy 数组（1D, 2D, 3D, 4D...）
  2. ✅ 支持压缩（gzip, lzf 等），节省存储空间
  3. ✅ 支持部分读取（不需要加载整个文件，可以按需读取切片）
  4. ✅ 可以存储多个数据集（多个数组）
  5. ✅ 可以存储元数据（metadata）
  6. ✅ 跨平台、跨语言支持

在这个项目中，H5 文件存储的是：
  - 单个 3D 数组（形状: D × H × W）
  - 数据集名称: 'data'
  - 数据类型: uint8 (0-255，归一化后的值）
  - 压缩方式: gzip
  - 来源: preprocess.py 处理后的裁剪体积

【步骤 7】H5 文件部分读取演示
--------------------------------------------------------------------------------
可以按需读取特定切片，而不需要加载整个文件：
  读取第 0 层: dataset[0, :, :]  → 形状 (466, 466)
  读取第 134 层: dataset[134, :, :]  → 形状 (466, 466)
  读取第 269 层: dataset[269, :, :]  → 形状 (466, 466)
  读取最后 1 层: dataset[-1, :, :]  → 形状 (466, 466)
  读取中间区域: dataset[269, 116:349, 116:349]  → 形状 (233, 233)

✅ 这种部分读取能力非常适合处理大型 3D 数据！

【步骤 8】与 preprocess.py 的关联
--------------------------------------------------------------------------------
这个 H5 文件是如何生成的（preprocess.py 流程）：

    1. 累积 3D 数组：
       d_slice_cropped_array = np.zeros((crop_d, crop_h, crop_w), dtype=np.float32)
       # 逐片读取 TIF 文件，裁剪并归一化...
       for i in range(crop_d):
           d_slice = read_image(slice_path).astype(np.float32)
           d_slice_cropped = d_slice[h_start:h_end, w_start:w_end]
           d_slice_cropped = ((d_slice_cropped - global_min) / (global_max - global_min)) * 255
           d_slice_cropped_array[i] = d_slice_cropped
    
    2. 保存为 H5：
       with h5py.File("cropped_volume_ROI.h5", "w") as f:
           f.create_dataset("data", data=d_slice_cropped_array.astype(np.uint8), compression="gzip")
    
    3. H5 文件内容：
       - 就是一个 3D NumPy 数组
       - 存储在名为 "data" 的数据集中
       - 形状: (D, H, W) = 深度 × 高度 × 宽度
       - 类型: uint8 (0-255，归一化后的值)
       - 压缩: gzip（节省存储空间）
    

================================================================================
✅ 总结：H5 文件就是存储 3D 数组的格式！
================================================================================
  当前文件: data/project_test/history_001/preprocess/full/cropped_volume_FULL.h5
  文件大小: 87.23 MB
  数据形状: (538, 466, 466)
  数据类型: uint8 (0-255)
  压缩方式: gzip
================================================================================
